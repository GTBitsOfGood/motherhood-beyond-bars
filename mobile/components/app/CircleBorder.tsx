import React from "react";
import { View } from "react-native";

const Circle = () => {
  return (
    <View
      style={{
        marginLeft: 8,
        width: 12,
        height: 12,
        borderRadius: 6,
        borderColor: "#304CD1",
        borderWidth: 1,
      }}
    ></View>
  );
};

export default Circle;
